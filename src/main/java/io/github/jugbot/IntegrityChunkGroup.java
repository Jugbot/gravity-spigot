package io.github.jugbot;

// Temporary object for calculating cross-chunk integrity
public class IntegrityChunkGroup {
  
  IntegrityChunkGroup(Iterable<IntegrityChunk> chunks) {
    for (IntegrityChunk chunk : chunks) {

    }
  }
}